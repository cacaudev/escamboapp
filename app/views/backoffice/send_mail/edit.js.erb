$(function() {
  $('#recipient-text').val('<%= @admin.email %>');
  $('h4.modal-title').text("<%= t('labels.new_message_to_label', name: @admin.name) %>");
  $('#sendMailModal').modal();
  $('#sendMailModal').on('shown.bs.modal', function () {
    $('#subject-text').focus();
  })
});