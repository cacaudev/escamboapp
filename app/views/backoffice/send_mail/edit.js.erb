$(function() {
  $('#recipient-text').val('<%= @admin.email %>');
  $('h4.modal-title').text('Nova mensagem para <%= @admin.name %>');
  $('#sendMailModal').modal();
  $('#sendMailModal').on('shown.bs.modal', function () {
    $('#subject-text').focus();
  })
});